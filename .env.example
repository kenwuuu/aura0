# copy the below into .env and then go get these keys from your accounts

# DO NOT commit this file to your repository!
SENTRY_ENVIRONMENT="dev"

#
IS_DEV_ENVIRONMENT=true

# cloudflare TURN
CLOUDFLARE_TURN_TOKEN_ID=
CLOUDFLARE_TURN_API_TOKEN=
#curl \
#	-H "Authorization: Bearer <CLOUDFLARE_TURN_API_TOKEN>" \
#	-H "Content-Type: application/json" -d '{"ttl": 86400}' \
#	https://rtc.live.cloudflare.com/v1/turn/keys/<CLOUDFLARE_TURN_TOKEN_ID>/credentials/generate-ice-servers
VITE_POSTHOG_PROJECT_TOKEN=phc_yVFqMSYG88kEXYf4vcMJgS7YuHpjRyYCD4aWicRXuJtF
VITE_POSTHOG_HOST=https://us.i.posthog.com

# Yjs WebSocket relay. Leave unset for the production relay
# (wss://digitalocean-ws-ipv4.aura0.app). Set it to test a staged relay from a real
# browser before it serves anyone — see networking/websocket/README.md:
#   ssh -L 47965:127.0.0.1:47965 root@<droplet>
#   VITE_WS_SERVER_URL=ws://127.0.0.1:47965 npm run dev
#VITE_WS_SERVER_URL=
