Framework-agnostic primitives: `components/` (generic React building blocks), `ui/` (shadcn), `utils/` (helpers).
Must not depend on `features/`, `services/`, or `infrastructure/` — strictly leaves of the dependency graph.
