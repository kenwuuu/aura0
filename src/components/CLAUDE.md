Cross-feature React components — top-level modals (welcome, help, hotkeys, patch notes, announcement, mobile warning), the add-card flow, board inverter, and room connection status.
Feature-specific UI belongs under `features/<feature>/`; generic primitives belong under `shared/`. Code here should be reused by more than one feature.
