Small app-level utilities (`centerHtmlElementOnDrag`, `confirmation`) — currently duplicated with `shared/utils/`.
Prefer `shared/utils/` for new helpers; this directory is being drained as part of the screaming-architecture migration.
